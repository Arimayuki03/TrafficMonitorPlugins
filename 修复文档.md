# TrafficMonitorPlugins 代码审查修复文档

> 基于对全部 10 个插件及共享工具库(约 1.6 万行 C++)的代码审查结论,按严重程度排序修复。
> 每个修复项标注:位置、问题、修复方案、状态。
> 验证方式:使用 VS2022 BuildTools (v143) 编译整个解决方案,确保无编译错误。

## 严重程度定义

- **P0**:可导致宿主进程(TrafficMonitor)崩溃或数据损坏
- **P1**:功能错误、确定性资源泄漏、数据竞争
- **P2**:低风险、边缘情况、代码质量

---

## 修复清单

### P0 级

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-001 | `DateTime/PluginSystemDate.cpp:37`、`PluginSystemTime.cpp:37` | `GetItemValueSampleText` 返回按值临时 `CString` 的内部指针,临时对象析构后宿主读已释放内存(use-after-free) | 将采样格式化结果保存到 `CDataManager` 成员后再返回其指针 | ✅ |
| FIX-002 | `TextReader/HtmlToText.cpp:101` | `html.size() - 4` 在长度不足 4 时下溢为极大数,`substr` 越界抛未捕获 `std::out_of_range`,终止宿主进程 | 改写循环条件为无下溢的等价形式 `index + 4 < html.size()`,并在函数入口处理空串 | ✅ |
| FIX-003 | `TextReader/Common.cpp`、`Stock/Common.cpp`、`Weather/Common.cpp` 的 `GetURL` | 将 Unicode 构建的 `CString`(宽字符缓冲)按 `const char*` 重解释塞入 `std::string`,内容可能被截断并与 FIX-002 构成崩溃链;中文内容还依赖系统区域设置 | 改用 `CInternetFile::Read` 按原始字节读入 `std::string`,不经过 `CString` | ✅ |

### utilities(共享工具库)

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-004 | `utilities/bass64/base64.cpp` | ① `IsBase64Code` 用 256 的循环上限读 65 字节的 `base64EncodeTable`,越界读约 191 字节;② `IsBase64Code`/`Base64Decode` 按值收 `std::string`(TextReader 对 50MB 内容调用,翻倍拷贝);③ 每次调用重建 `std::set`;④ `i <= max_length` 差一 | ①循环上限改为编码表长度;②③改为 `const std::string&` 并用查表法替代 set;④改 `<` | ✅ |
| FIX-005 | `utilities/Common.cpp` | ① 返回裸 `new[]` 指针的 `GetFileContent` 重载无调用方且易泄漏;② 字符串版 `GetFileContent` 未检查 `read` 结果 | ①删除死代码重载;②检查 `gcount`,长度为 0 时正确返回 | ✅ |
| FIX-006 | `utilities/FilePathHelper.cpp:105` | `GetFilePathWithoutExtension` 在路径任意位置的 `.` 都截断(`C:\my.dir\file` → `C:\my`),未限定点号必须在最后一段路径内 | 仅当最后一个 `.` 位于最后一段路径(最后一个分隔符之后)时才截断 | ✅ |
| FIX-007 | `utilities/IniHelper.cpp:140-158` | `WriteBoolArray` 位掩码 `(1 << i)` 对 i≥31 有符号溢出(UB)且仅支持 32 个 bool;`GetBoolArray` 忽略 `default_value` 参数 | 改用无符号移位并加入 size 上限断言/文档;`GetBoolArray` 空键时返回默认值 | ✅ |

### DateTime 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-008 | `PluginSystemDate.cpp:36`、`PluginSystemTime.cpp:36` | `wMinute = 59` 连续赋值两次,本意为 `wSecond = 59` | 修正为 `wSecond` | ✅ |
| FIX-009 | `DateTime.cpp:41-49` | `GetInfo` 所有分支共用一个 `static CString`,第二次调用会改写第一次返回指针指向的内容 | 每个信息索引使用独立静态缓冲 | ✅ |
| FIX-010 | `DateTime.rc:385、393` | 英文月份缩写拼错:`"Ari"`→`Apr`、`"Aut"`→`Aug` | 修正资源字符串 | ✅ |
| FIX-011 | `DateTime/MessageDlg.cpp:55-56` | 每次打开帮助对话框 `LoadImage` 一个 HICON 从不销毁 | 保存 HICON,在对话框销毁时 `DestroyIcon` | ✅ |
| FIX-012 | `DateTime/MessageDlg.h:25`、`OptionsDlg.h:22` | `m_min_size` 未初始化,WM_GETMINMAXINFO 在 WM_INITDIALOG 前触发时读到栈垃圾 | 类内初始化 `{0,0}` | ✅ |

### Battery 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-013 | `Battery/BatteryItem.cpp:12-17` | 动画定时器在静态对象构造(DllMain/加载器锁)中创建,返回 ID 被丢弃且永不销毁 | 定时器创建移至 `OnExtenedInfo(EI_CONFIG_DIR)` 时机(首次使用时),保存 ID,析构时 `KillTimer` | ✅ |
| FIX-014 | `Battery/DataManager.cpp:19-20` | `GdiplusStartup` 无配对的 `GdiplusShutdown` | 析构中调用 `GdiplusShutdown(m_gdiplusToken)` | ✅ |
| FIX-015 | `Battery/DataManager.cpp:137-154` | `BatteryLifePercent==255`(未知)时显示 "255 %" | 255 时按无电池/N-A 处理 | ✅ |
| FIX-016 | `Battery/Battery.cpp:43` | `GetSystemPowerStatus` 返回值未检查,失败时显示全 0 | 检查返回值,失败保持 N/A | ✅ |
| FIX-017 | `Battery/DataManager.cpp:110-124` | `LoadImage` 失败时把 NULL 缓存进 `m_icons` 永不重试 | 仅缓存非空句柄 | ✅ |

### PluginTemplate 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-018 | `PluginTemplate/PluginTemplateItem.cpp:13`、`PluginTemplate.cpp:102` | 模板默认 `GetItemId` 返回空串;配置文件名残留 `KeyboardIndicator.ini`(照搬即与 KeyboardIndicator 插件互相覆盖配置) | 给出安全占位默认值 `L"TemplateItem"` 与 `L"PluginTemplate.ini"` | ✅ |

### IpAddress 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-019 | `IpAddress/IpAddress.cpp:34-37` | `DataRequired` 空置,IP 永不自动刷新;`AdapterCommon.cpp:59 RefreshIpAddress` 为无调用死代码 | `DataRequired` 中按 10 秒节流调用适配器刷新 | ✅ |
| FIX-020 | `IpAddress/IpAddress.cpp:29-32` | `GetTooltipInfo` 永远返回空串(`m_tooltip_info` 从未赋值) | 刷新时同步生成提示文本 | ✅ |
| FIX-021 | `IpAddress/AdapterCommon.cpp:19-29` | `GetAdaptersInfo` 缓冲区不足仅重试一次 | 改为最多重试 3 次的循环 | ✅ |
| FIX-022 | `IpAddress/DataManager.cpp:102-116` | `LoadImage` 失败结果被永久缓存;HICON 从不释放 | 仅缓存非空句柄;析构时 `DestroyIcon` | ✅ |
| FIX-023 | `IpAddress/IpAddressItem.cpp:22-24` | const 方法内写函数级 `static std::wstring`,多线程隐患且语义不清 | 改为成员变量,在刷新时更新 | ✅ |

### KeyboardIndicator 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-024 | `KeyboardHook.cpp:83` | `SetWindowsHookExW` 失败未处理,线程照常空转,实时按键功能静默失效且无法自愈 | 失败时通知线程退出,使 `Start()` 可重试 | ✅ |
| FIX-025 | `KeyboardHook.cpp:40-45、67-74` | `Stop()` 发 `WM_QUIT` 后不等待线程退出即删除临界区(UB 风险);线程句柄不关闭 | 等待线程退出(带超时)后再删临界区,关闭句柄,重置 `m_thread_id` | ✅ |
| FIX-026 | `KeyboardHook.cpp:167-174` | Ctrl/Shift/Alt/Win 丢失 KEYUP 后永不清除,任务栏持续显示 | 修饰键同样应用 5 分钟超时清理 | ✅ |
| FIX-027 | `KeyboardHook.cpp:128-143` | `AddKey` 每次按键(含自动重复)都先 `GetKeyName` 再在锁内发现重复而返回,钩子回调内做无谓 win32k 调用 | 先查重,仅新键才取键名 | ✅ |
| FIX-028 | `KeyboardIndicatorItem.cpp:102-126` | 使用 GDI+ 但从不 `GdiplusStartup`,当前能工作纯靠宿主初始化了 GDI+ | 在 `CDataManager` 初始化/析构中配对调用 GdiplusStartup/Shutdown | ✅ |
| FIX-029 | `KeyboardHook.cpp:47-64` | `Start()` 对线程句柄的检查-重建无同步 | 纳入 `m_cs` 保护 | ✅ |
| FIX-030 | `KeyboardIndicator.cpp:73` | `GetInfo` 内未使用 `static CString` 死代码 | 删除 | ✅ |

### LoudnessMeter 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-031 | `LoudnessMeter.cpp:11-14、165-171` | COM 初始化在静态对象构造(DllMain)中执行且无配对释放;"重新初始化"命令重复执行泄漏 COM 接口;`CoCreateInstance` 失败后该命令对空 `pEnumerator` 解引用崩溃 | COM 初始化/设备创建移至 `OnInitialize`;`InitDevice` 先 Release 旧接口、判空防御;析构尽力释放 | ✅ |
| FIX-032 | `LoudnessMeter.cpp:101-103` | 50ms 定时器永不销毁,且每次 `EI_CONFIG_DIR` 重复创建 | 保存定时器 ID,创建前先销毁旧定时器,析构时 `KillTimer` | ✅ |
| FIX-033 | `LoudnessMeterItem.cpp:172-173` | `SelectObject(&m_font)` 选入宿主 DC 后不恢复,宿主 DC 字体状态被永久改动 | 保存旧对象并在绘制结束前恢复 | ✅ |
| FIX-034 | `LoudnessMeterItem.cpp:185-186` | 使用 GDI+ 但从不 `GdiplusStartup` | 配对初始化/反初始化 | ✅ |
| FIX-035 | `LoudnessMeter.cpp:148、45-48` | `GetPeakValue` HRESULT 未检查,设备失效时只能手动重初始化;`m_tooltip_info` 从未赋值 | 检查 HRESULT 并标记无效态;采集时同步生成提示文本 | ✅ |

### TextReader 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-036 | `TextReader/DataManager.cpp:344-356、165-169` | ① 后台线程写 `m_text_contents`/章节表,UI 线程并发读,无任何同步;② `static std::wstring url` 在线程运行中可被重赋值;③ `m_is_thread_runing` check-then-act 竞争,可起两个并发写线程 | ① 为共享数据加 `std::mutex`,读侧(DrawItem/tooltip/翻页/书签/章节)全部加锁;② 改为线程堆分配参数、线程内释放;③ 置位提前到起线程前并用 atomic | ✅ |
| FIX-037 | `TextReader/DataManager.cpp:221-222`、`Common.cpp:5-18、40` | 编码识别仅 GBK/UTF-8,UTF-16LE/BE 必然乱码;带 BOM 的 UTF-8 残留 U+FEFF | 增加 BOM 检测(UTF-16LE/BE/UTF-8),UTF-16 按宽字节直接转换,BOM 剥离 | ✅ |
| FIX-038 | `TextReader/DataManager.cpp:196-211` | `file.read` 结果未检查;`tellg` 异常值(目录/特殊文件)时把未初始化内存当文本 | 检查 `read`/`gcount`,长度为 0 或越界时按失败处理 | ✅ |
| FIX-039 | `TextReader/BookmarkDlg.cpp:77`、`TextReader.cpp:43` | 文本为空时以 0 为除数,显示 `inf%`/`nan%` | 除零保护 | ✅ |
| FIX-040 | `TextReader/BookmarkMgr.cpp:17-18` | 书签读取缓冲区仅 MAX_PATH(260),长路径/多书签静默截断 | 扩大缓冲区(4096) | ✅ |
| FIX-041 | `TextReader/TextReader.cpp:113-121` | `CheckMenuItem` 先解引用后判空 | 调整判空顺序 | ✅ |
| FIX-042 | `TextReader/TextReaderItem.cpp:133-141` | `OnKeboardEvent` VK_DOWN 分支漏 `return 1`,宿主重复处理方向键 | 补 `return 1` | ✅ |
| FIX-043 | `TextReader/HtmlToText.cpp:36` | `ParseFromFile` 成功路径 `return false`,返回值恒错 | 修正返回值 | ✅ |
| FIX-044 | `TextReader/ChapterDlg.cpp:96` | `index <= m_chapter_index.size()` 差一越界(潜伏) | 改为 `<` | ✅ |

### HardwareMonitor 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-045 | `HardwareMonitorHelper.cpp:141` + `HardwareMonitorItem.cpp:118` | 传感器 `Value` 为 null 时 `Nullable.Value` 抛异常,`DataRequired` 项目循环中断,后续所有监控项数值冻结 | `GetSensorValue` 检查 `HasValue`;调用点加保护 | ✅ |
| FIX-046 | `HardwareMonitor/SettingsForm.cpp:206-233` | 点"取消"也保存配置并实时生效 | 仅在 OK 路径保存;`OnFormClosing` 判断标志 | ✅ |
| FIX-047 | `HardwareMonitor/SettingsForm.cpp:311-390` | ListBox 索引与 `items_info` 索引错位,存在传感器缺失项时删错/移错条目 | 维护显示索引→配置索引映射表 | ✅ |
| FIX-048 | `HardwareMonitor.cpp:60-100` | 选项对话框增删监控项后 `m_items` 不重建;且直接重建 vector 会使宿主缓存的 `IPluginItem*` 悬空 | 条目对象改为堆分配池(只增不减,移除仅从枚举列表摘除并标记),重建枚举列表时保持已分配对象稳定;受宿主缓存机制限制,主窗口仍需重启生效,文档记录该限制 | ✅ |
| FIX-049 | `HardwareMonitorHelper.cpp:161-165、193-198` | MHz→GHz、mWh→Wh 用 `/1024`(应为 /1000),系统性偏差 2.4% | 修正换算系数 | ✅ |
| FIX-050 | `HardwareMonitorHelper.cpp:294-325` | `GetDefaultMonitorItem` 找到 GPU 温度后 `break` 退出整个循环,负载传感器漏加 | 仅跳出该硬件的传感器遍历(改用标志位),不跳出硬件循环 | ✅ |
| FIX-051 | `HardwareMonitor.cpp:394-397、506-519` | `LoadIcon` 失败被缓存,`GetAppIcon()->Handle.ToPointer()` 对 nullptr 解引用崩溃 | 图标为空时返回 nullptr,不做解引用 | ✅ |
| FIX-052 | `HardwareMonitorItem.cpp:21-26、41-75` | `GetItemId`/`GetItemValueSampleText` 用函数级 static 缓冲,所有实例共享互相覆盖 | 改为成员变量 | ✅ |
| FIX-053 | `HardwareMonitor.cpp:17-22、294、307、316-337` | 构造函数 `Graphics::FromHwnd` 未 Dispose;`GetInfo` 死代码与 static 缓冲 | 修复/清理 | ✅ |
| FIX-054 | `SettingsForm.cpp:269`、`HardwareInfoForm.cpp:196-201` | 自绘路径每次 `gcnew SolidBrush` 不释放,依赖 GC 终结器 | `delete` 托管 Brush | ✅ |

### Stock 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-055 | `StockDef.cpp:275-304`、`DataManager.cpp:158-161` | `stocks` map 及 `StockInfo::info`(含 `std::wstring`)后台线程写/UI 线程读完全无同步,节点失效可崩溃 | 互斥量改为 `std::recursive_mutex`,统一保护 map 与 info 的读写;UI 读取点全部加锁 | ✅ |
| FIX-056 | `Stock/FloatingWnd.cpp:320-335` | 网络线程持窗口裸指针,窗口销毁后 use-after-free;`m_isDestroying` 全工程无读取点 | 线程启动即复制 `stock_id`,结果写入全局 DataManager,窗口更新一律 `PostMessage`;销毁前置位 `m_isDestroying` 并在线程收尾检查 | ✅ |
| FIX-057 | `Stock/DataManager.cpp:214-243` | `RequestTimelineData` 成功路径泄漏 `CInternetSession`,catch 路径连 `pFile` 泄漏 | 所有路径完整清理 | ✅ |
| FIX-058 | `StockDef.cpp:364-402` | `LoadTimelineDataByJson` 的 yyjson 文档所有路径不释放 | 所有路径 `yyjson_doc_free` | ✅ |
| FIX-059 | `Stock.cpp:88-90` | `GetItem` 未拦截负索引,违反宿主契约 | `index < 0 \|\| index >= size` 返回 nullptr | ✅ |
| FIX-060 | `Stock.cpp:216-219` | `updateItems` 按值遍历,复位失效 | 改引用遍历 | ✅ |
| FIX-061 | `FloatingWnd.cpp:244、258` | `prevClosePrice==0` 时涨跌幅显示 `inf` | 除零保护 | ✅ |
| FIX-062 | `ManagerDialog.cpp:149-152` | K 线宽高 `_ttoi` 无校验,可产生 0/负尺寸窗口 | 钳制到合理范围 | ✅ |
| FIX-063 | `Stock.cpp:62-67、33` | worker 线程跨线程 `EnableMenuItem` 操作 UI 菜单;`m_is_thread_runing`/`m_last_request_time` 无同步 | 菜单状态改为 UI 线程在弹出菜单前按原子标志刷新;标志改 `std::atomic<bool>` | ✅ |
| FIX-064 | `StockItem.cpp:13、36-37` | `GetItemName`/`GetItemId` 用函数级 static 缓冲,10 个 item 互相覆盖 | 改为成员变量 | ✅ |
| FIX-065 | `Stock.cpp:100-104`、`DataManager.cpp:163-169`、`resource.h:16-17` | 死代码(`last_req_time`)、窗口程序中 `std::cout` 与重复 `srand`、资源 ID 重号 | 清理/重排 | ✅ |

### Weather 插件

| 编号 | 位置 | 问题 | 修复方案 | 状态 |
|---|---|---|---|---|
| FIX-066 | `Weather.cpp:196、205` | `str_date.substr(11)` 无长度守卫,畸形响应抛未捕获异常终止进程;`CTime` 可用非法年月构造 | 补长度守卫;年月日合法性校验,非法则拒绝更新 | ✅ |
| FIX-067 | `Weather.cpp:161-230` | `ParseJsonData` 提前返回路径泄漏 yyjson 文档 | 所有路径 `yyjson_doc_free` | ✅ |
| FIX-068 | `Weather.cpp:27-73` 等全部共享状态 | 天气数据/tooltip/城市/历史 map 后台线程写、UI 线程读,无任何互斥 | 引入 `std::recursive_mutex` 保护,写侧与所有 UI 读取点加锁 | ✅ |
| FIX-069 | `Weather.cpp:66-67、332-334、421-423、444-453` | worker 线程解引用 UI 线程栈上对话框指针(悬空竞争),并跨线程操作对话框控件/菜单 | 改存 `HWND`,经 `PostMessage` 通知 UI 线程处理 | ✅ |
| FIX-070 | `Weather.cpp:402-404、308-323` | `m_is_thread_runing` 普通 bool 竞争;`DataRequired` 每秒起一个立即退出的线程 | 改 `std::atomic<bool>`;起线程前检查 3 秒门限 | ✅ |
| FIX-071 | `HistoryWeatherMgr.cpp:111-114` | 捕获 `CArchiveException*` 未 `Delete()` | 补 `Delete()` | ✅ |
| FIX-072 | `Weather.cpp:246-253`、`HistoryWeatherMgr.cpp:124-135` | 历史天气数据无界增长 | 保存/加载时仅保留最近 30 天 | ✅ |
| FIX-073 | `Weather/Common.cpp` 所在接口 | 天气主接口使用明文 http,存在劫持风险 | 改为 https | ✅ |
| FIX-074 | `Weather.cpp:59-60`、`resource.h:34-35` | `ofstream` 打开失败静默;资源 ID 重号 | 检查打开结果;重排资源 ID | ✅ |

---

## 已知限制(记录,不在本次修复范围)

1. **HardwareMonitor 插件增删监控项在主窗口需重启宿主才能生效**:宿主在启动时一次性缓存 `GetItem` 返回的指针(FIX-048 已保证缓存指针永不悬空,且任务栏窗口重建时会取到新列表)。
2. **yyjson 0.4.0 未升级**:建议后续单独评估升级到最新版。
3. **Stock/Weather 中文名称依赖系统区域设置(ACP=936)**:数据源以 GBK 返回股票名,现有 `StrToUnicode(CP_ACP)` 行为保留;彻底解决需按 HTTP 头/内容嗅探编码。
4. **各插件 GDI 句柄随进程存活**:`HICON` 等在宿主进程退出前不释放,无实际影响,未逐一处理。

## 验证

- 使用 VS2022 BuildTools (v143) 对 `TrafficMonitorPlugins.sln` 执行 Release|x64 编译,确保零错误(新增代码含 `<mutex>`、`<atomic>` 等,均为标准库)。
- 行为级验证项(需人工):DateTime 选项预览宽度、TextReader 打开 URL/本地文件、Stock 悬浮窗刷新与关闭、Weather 刷新与自动定位、KeyboardIndicator 实时按键、LoudnessMeter 重新初始化。

---

## 修复结果(2026-09-13)

### 编译验证

- 环境:VS2022 BuildTools (v143),Release|x64。
- **11 个项目编译通过**:`utilities`、`Battery`、`DateTime`、`IpAddress`、`KeyboardIndicator`、`LoudnessMeter`、`PluginTemplate`、`TextReader`、`Stock`、`Weather`、`PluginTester`,产物位于 `bin/x64/Release/` 与 `lib/x64/Release/`。
- **`HardwareMonitor` 未能在本机编译**:本机 BuildTools 未安装 .NET Framework 4.7.2 开发人员包(MSB3644),属环境限制而非代码问题。该项目为 C++/CLI,需要在装有 .NET 4.7.2 SDK 的环境(如原仓库 AppVeyor CI 或完整版 VS2022)中编译验证。

### 与原计划的差异说明

1. **FIX-046(HardwareMonitor 设置对话框)**:审查中发现该窗体实际是"即时生效"式设计——所有监控项属性在控件事件中直接写入 `items_info`,且窗体上没有"确定"按钮(只有"取消"直接关闭)。因此无法实现真正的"取消回滚"。本次修复改为:窗体关闭后一律保存配置并向宿主返回 `OR_OPTION_CHANGED`(原先该返回值永远不可能出现,导致宿主从不刷新显示),在文档中记录此设计限制。
2. **FIX-070(Weather DataRequired 节流)**:复查确认原实现已有 30 分钟节流,实际问题是检查-置位的竞争窗口;修复为 `std::atomic` + CAS,未改动节流逻辑。
3. **FIX-074(Weather 资源 ID 重号)**:`IDS_DAYS_AGO2`(119)在 .rc 中完全未使用,与 `IDS_DATE_FORMAT` 的重号仅为名义冲突,不产生实际资源冲突;已回退重编号,保持与上游 diff 最小。
4. **FIX-057 附带**:Stock 的 `RequestRealtimeData` 中的 `generateRandomDouble` 移除了窗口程序中的 `std::cout`(随 FIX-065 一并处理)。
5. **额外加固**(未在原清单中,顺带完成):三个 GetURL 均改为按原始字节读取后统一去除 `
`,保持与原"按行读取拼接"完全一致的行为,消除对 MFC 内部实现的依赖。

### 修改文件清单

utilities(base64.h/base64.cpp、Common.h/Common.cpp、FilePathHelper.cpp、IniHelper.cpp);
DateTime(DataManager.h、PluginSystemDate.cpp、PluginSystemTime.cpp、DateTime.cpp、MessageDlg.h/cpp、OptionsDlg.h、DateTime.rc);
Battery(BatteryItem.h/cpp、DataManager.cpp、Battery.cpp);
PluginTemplate(PluginTemplate.cpp、PluginTemplateItem.cpp);
IpAddress(IpAddress.h/cpp、DataManager.h/cpp、IpAddressItem.cpp、AdapterCommon.cpp);
KeyboardIndicator(KeyboardHook.h/cpp、DataManager.h/cpp、KeyboardIndicator.cpp);
LoudnessMeter(LoudnessMeter.h/cpp、LoudnessMeterItem.cpp、DataManager.h/cpp);
TextReader(DataManager.h/cpp、Common.h/cpp、HtmlToText.cpp、TextReader.cpp/cpp项、BookmarkDlg.cpp、BookmarkMgr.cpp、TextReaderItem.cpp、ChapterDlg.cpp);
HardwareMonitor(HardwareMonitor.h/cpp、HardwareMonitorItem.h/cpp、HardwareMonitorHelper.cpp、SettingsForm.h/cpp、HardwareInfoForm.cpp);
Stock(Stock.h/cpp、StockDef.cpp、DataManager.cpp、StockItem.h/cpp、FloatingWnd.h/cpp、ManagerDialog.cpp、Common.cpp);
Weather(Weather.h/cpp、DataManager.cpp、OptionsDlg.h/cpp、WeatherItem.cpp、WeatherHistoryDlg.cpp、HistoryWeatherMgr.h/cpp、Common.cpp)。
